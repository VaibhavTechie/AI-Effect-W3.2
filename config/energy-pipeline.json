{
  "containers": [
    {
      "id": "step1",
      "command": "docker run --rm alpine sh -c \"echo hello > /data/hello.txt\""
    },
    {
      "id": "step2",
      "command": "docker run --rm alpine sh -c \"wc -c < /data/hello.txt > /data/len.txt\""
    }
  ]
}
